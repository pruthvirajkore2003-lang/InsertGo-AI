{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "improvechip",
  "description": "Capability for the Inline Improve progress chip window (event listener only — the chip renders status, Rust owns show/hide).",
  "windows": ["improvechip"],
  "permissions": [
    "core:default",
    "core:event:default",
    "log:default"
  ]
}
